export const selectRound = (state) => state.round;

export const selectPlayerName = (state) => state.player_name;

export const selectBoard = (state) => state.board;

export const selectInitialized = (state) => state.initialized;

export const selectIsOver = (state) => state.is_over;
